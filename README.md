# Student_placement_predictor
A Machine Learning project built using Python, Pandas, NumPy, Scikit-learn, and Flask that predicts student placement based on academic and skill-related features. The model is trained on historical placement data and deployed through a simple, user-friendly web interface.
